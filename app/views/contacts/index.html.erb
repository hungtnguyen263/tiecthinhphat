<!-- Page header -->
<div class="page-header">
    <div class="page-header-content">
        <div class="page-title">
            <h4><a href="<%= admins_path %>" class="btn-back"><i class="icon-arrow-left52 position-left"></i></a> <span class="text-semibold">Liên hệ</span> - Quản lý</h4>
        </div>
    </div>

    <div class="breadcrumb-line">
        <ul class="breadcrumb">
            <li><a href="<%= admins_path %>"><i class="icon-home2 position-left"></i> Trang chủ</a></li>
            <li><a href="<%= contacts_path %>">Quản lý</a></li>
            <li class="active">Quản lý tin nhắn đến</li>
        </ul>
    </div>
</div>
<!-- /page header -->

<!-- Content area -->
<div class="content">

    <!-- Basic datatable -->
    <div class="panel panel-flat">
        <div class="panel-heading">
            <hr/>
            <h5 class="panel-title">Quản lý <span class="text-semibold">TIN NHẮN ĐẾN</span></h5>
            <div class="heading-elements">
                <ul class="icons-list">
                    <li><a data-action="collapse"></a></li>
                    <li><a data-action="reload"></a></li>
                    <li><a data-action="close"></a></li>
                </ul>
            </div>
        </div>
        
        <table class="table datatable-contact">
            <thead>
                <tr>
                    <th>Họ & tên</th>
                    <th>Điện thoại</th>
                    <th>Email</th>
					<th>Tiêu đề/Chủ đề</th>
                    <th>Lời nhắn</th>
                    <th>Ngày gửi</th>
                    <th class="text-center">Thao tác</th>
                </tr>
            </thead>
            <tbody>
                <% @contacts.each do |item| %>
                    <tr>
                        <td><%= link_to (truncate(strip_tags(item.name), length: 20)), item %></td>
                        <td><%= item.phone %></td>
						<td><%= item.email %></td>
						<td><%= item.title %></td>
                        <td><%= truncate(strip_tags(item.message), length: 50) %></td>
                        <td>
							<%= item.created_at.strftime("%d/%m/%Y") %>
						</td>
                        <td class="text-center">
                            <ul class="icons-list">
                                <li class="dropdown">
                                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                                        <i class="icon-menu9"></i>
                                    </a>
    
                                    <ul class="dropdown-menu dropdown-menu-right">
                                        <li>
                                            <%= link_to '<i class="icon-cross2"></i> Xóa'.html_safe, item, method: :delete, data: {confirm: 'Bạn có thực sự muốn xóa?'} %>
                                        </li>
                                    </ul>
                                </li>
                            </ul>
                        </td>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </div>
    <!-- /basic datatable -->

    <!-- Footer -->
	<div class="footer text-muted">
		Copyright &copy; 2016 - Dịch Vụ Tiệc Thịnh Phát.
	</div>
	<!-- /footer -->

</div>
<!-- /content area -->

<!-- Theme JS files -->
<%= javascript_include_tag '/assets/js/plugins/forms/styling/uniform.min.js' %>
<%= javascript_include_tag '/assets/js/plugins/tables/datatables/datatables.min.js' %>
<%= javascript_include_tag '/assets/js/plugins/forms/selects/select2.min.js' %>

<%= javascript_include_tag '/assets/js/core/app.js' %>
<%= javascript_include_tag '/javascripts/datatables_contact.js' %>
<!-- /theme JS files -->